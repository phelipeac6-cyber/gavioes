import { PageLayout } from "@/components/PageLayout";

const MyInfo = () => {
  return (
    <PageLayout title="Minhas Informações">
      <div className="text-center">
        <p>Página para editar informações do perfil.</p>
        {/* Formulário para editar informações do perfil será adicionado aqui */}
      </div>
    </PageLayout>
  );
};

export default MyInfo;